date: 2026-03-15

======

## time: ~now

### Mục tiêu
QA/QC: Triển khai Contract Tests (Pattern F) và Cross-Function Flow Tests (Pattern G) theo skill `06_Skills/unit_test_creation.md`.

### Thay đổi

**Contract tests (00_test/contract/) — 70 tests, 5 files:**
- `test_contract_queue_message.py` — C1: Queue Message (10 tests, CT-001→CT-005)
- `test_contract_job_status.py` — C2: Job Status (16 tests, CT-006→CT-011)
- `test_contract_file_proxy.py` — C3+C4: File Download/Upload (21 tests, CT-012→CT-021)
- `test_contract_heartbeat.py` — C5: Heartbeat (12 tests, CT-022→CT-024)
- `test_contract_registration.py` — C6: Registration (11 tests, CT-025→CT-027)

**Flow tests (00_test/contract/) — 52 tests, 5 files:**
- `test_flow_happy_path.py` — F1: Happy Path (9 tests, FL-001→FL-003)
- `test_flow_retry.py` — F2: Retry Flow (12 tests, FL-004→FL-006)
- `test_flow_dlq.py` — F3: DLQ Flow (10 tests, FL-007→FL-009)
- `test_flow_registration.py` — F4: Registration+Approval (10 tests, FL-010→FL-012)
- `test_flow_aggregation.py` — F5: Multi-job Aggregation (11 tests, FL-013→FL-015)

**Infrastructure:**
- `00_test/contract/conftest.py` — Event loop, sys.path setup
- `00_test/contract/helpers.py` — Module loaders, pre-loaded modules, mock factories
- `00_test/run_all_tests.py` — Updated to include contract test suite

**Reports:**
- `05_test_cases/contract/QA_CONTRACT_FLOW_TEST_REPORT_2026-03-15.md`

### Kết quả
122 tests, 100% pass, 0 bugs. Execution time 0.22s. Tổng cộng dự án: 515 tests (170 backend + 223 worker + 122 contract/flow).

### Lý do
Bổ sung cross-function testing layer theo skill plan. Verify 6 backend↔worker contracts (field alignment, enum compatibility, data format round-trip) và 5 business flows (happy path, retry, DLQ, registration, multi-job aggregation).

======

## time: ~now

### Mục tiêu
Customize 2 bộ Skills cho phù hợp sâu với hệ thống OCR/IDP Platform.

### Thay đổi

**1. architecture-code-reviewer (`04_docs/architecture-code-reviewer/`) — 5 files rewrite:**
- `SKILL.md` — Rewrite toàn bộ: bỏ generic discovery, hardcode đúng 3-layer architecture (Edge→Orchestration→Processing), Controller→Service→Repo pattern, 6 modules, 39 API endpoints, DB schema 9 tables, NATS streams, MinIO buckets. Phase 1-4 đều OCR-specific.
- `references/review-checklist.md` — Checklist cho từng API endpoint, DB table, NATS subject, MinIO bucket. Job state machine transitions, upload validation limits.
- `references/patterns-catalog.md` — 6 categories anti-patterns đặc thù: Layer Bypass, Invalid State Transition, Lost Job, Infinite Retry Loop, File Proxy Bypass, OCR Model Reload Per Job, v.v.
- `references/report-template.md` — Template với bảng mapping cụ thể: 3-layer separation, per-module C→S→R compliance, job state machine, request aggregation.
- `references/language-specific.md` → `Stack-Specific Guide` — 6 sections: FastAPI, React, OCR Workers, NATS JetStream, MinIO, Docker.
- `scripts/scan-codebase.sh` — OCR-specific scan: 3-layer verification, module structure check, endpoint counting, security scan.

**2. integration-test-runner (`06_Skills/integration-test-runner/`) — 5 files rewrite:**
- `SKILL.md` — Rewrite: map đúng test infrastructure hiện có (00_test/backend, ocr_worker, contract), pattern hiện tại (load_module, factories, contract/flow test IDs). Integration points cụ thể (NATS, File Proxy, SQLite, MinIO). 10 critical scenarios, 10 edge cases.
- `references/framework-configs.md` — Setup guide đúng với pytest + importlib.util + mocked deps pattern. Configs cho backend/worker/contract/API integration/infrastructure tests.
- `references/test-strategies.md` — Strategies cho 6 loại: Contract (schema alignment), Flow (cross-module logic), API (FastAPI TestClient), SQLite repos, MinIO, NATS. Code patterns follow existing test structure.
- `references/troubleshooting.md` — OCR-specific: module loading errors, AsyncMock issues, state machine mismatches, contract schema drift, SQLite locked, NATS stream not found.
- `references/report-template.md` — Template với sections: Contract alignment, Flow results, API integration, Infrastructure. Report lưu tại `05_test_cases/{suite}/`.
- `templates/test-helpers.md` — Document existing helpers (conftest factories, contract helpers). New helpers: auth, upload, worker registration, SQLite/MinIO/NATS test utilities.

### Lý do
Bộ skills generic không phản ánh đúng architecture, patterns, và conventions của dự án OCR. Customize sâu giúp Claude Code review và test chính xác hơn: biết đúng 3-layer architecture, biết đúng 39 endpoints, biết đúng job state machine, biết đúng test patterns hiện có (load_module, factories, CT/FL test IDs).

======

## time: 22:00

### Mục tiêu
Phân tích toàn bộ codebase và viết integration tests cho hệ thống OCR Platform.

### Thay đổi

**Integration tests (00_test/integration/) — 68 tests, 4 files:**
- `test_repo_integration.py` — RP-001→RP-035: Repository CRUD với real SQLite in-memory (35 tests)
  - User, Session, Request, Job, File, ServiceType, ServiceInstance, AuditLog repos
  - Cascade delete, soft delete, pagination, filters, counters
- `test_api_auth_integration.py` — IA-001→IA-010: Auth API via FastAPI TestClient (10 tests)
  - Register, login, logout, /me, expired token, full auth flow
- `test_api_internal_integration.py` — II-001→II-015: Internal API endpoints (15 tests)
  - Worker registration (PENDING, APPROVED, REJECTED), heartbeat actions, job status updates, deregistration
- `test_lifecycle_integration.py` — LF-001→LF-008: Cross-cutting lifecycle flows (8 tests)
  - Job lifecycle, multi-job aggregation, cancellation, retry/DLQ, full E2E flow

**Infrastructure:**
- `00_test/integration/conftest.py` — In-memory SQLite + StaticPool, FastAPI TestClient, exception handlers
- `00_test/integration/helpers.py` — Shared helpers (create_user, create_request_with_jobs, etc.)
- `00_test/run_all_tests.py` — Updated to include integration suite

**Reports:**
- `05_test_cases/integration/QA_INTEGRATION_TEST_REPORT_2026-03-15.md`

### Kết quả
68 integration tests, 100% pass, 0 bugs. Execution time ~7s. Tổng cộng dự án: **583 tests** (170 backend + 223 worker + 122 contract/flow + 68 integration).

### Lý do
Bổ sung integration testing layer kiểm tra tương tác thực giữa Repository↔SQLite, API endpoints↔Service↔Repository, và cross-cutting flows (job lifecycle, worker registration, retry/DLQ). Dùng real SQLAlchemy + real bcrypt + FastAPI TestClient thay vì mocks.

======

## time: 08:15

### Mục tiêu
E2E Testing: Viết và chạy End-to-End tests kiểm tra toàn bộ luồng user flow với real services (Backend, MinIO, NATS). Worker được simulate qua internal API calls.

### Thay đổi

**E2E tests (00_test/e2e/) — 40 tests, 8 files:**
- `conftest.py` — Session-scoped fixtures: backend health check, admin/user token, worker registration+approval, sample files (PNG/PDF)
- `helpers.py` — Upload helpers (rate-limit retry), worker simulation (download→upload→status), polling helpers
- `test_e2e_health.py` — E2E-001: Health check (2 tests)
- `test_e2e_auth.py` — E2E-002→003, 014: Register, login, auth errors (8 tests)
- `test_e2e_upload_flow.py` — E2E-004→006, 010→011: Upload PNG/PDF, full lifecycle, multi-file, download result (7 tests)
- `test_e2e_worker_api.py` — E2E-007→008: Worker register, heartbeat, deregister (5 tests)
- `test_e2e_cancel.py` — E2E-013: Cancel request/job (2 tests)
- `test_e2e_errors.py` — E2E-012, 014, 015: Invalid MIME, no auth, not found (9 tests)
- `test_e2e_services.py` — Service discovery, request listing (2 tests)
- `test_e2e_admin.py` — Admin service type CRUD, dashboard stats (5 tests)

**Core test: Full OCR Lifecycle (test_e2e006)**
- User uploads PNG → Backend stores in MinIO → Job published to NATS → Worker (simulated) downloads via file-proxy → Uploads result via file-proxy → Marks COMPLETED → User checks status COMPLETED → User downloads result text

**Infrastructure:**
- Admin user created via CLI: `python3 -m app.cli create-admin`
- Worker simulated: register → admin approve → heartbeat → file-proxy download/upload → job status update
- Rate limit handling: retry with backoff for upload endpoint (10/60s limit)

**Reports:**
- `05_test_cases/e2e/E2E_TEST_REPORT_2026-03-15.md`

### Kết quả
40 E2E tests, 100% pass, 0 bugs. Execution time ~30s. Tổng cộng dự án: **623 tests** (170 backend + 223 worker + 122 contract/flow + 68 integration + 40 E2E).

### Lý do
Kiểm tra toàn bộ user flow end-to-end với real services (Backend, MinIO, NATS). Xác nhận luồng: User upload → Backend xử lý → NATS queue → Worker nhận và xử lý → File Proxy truyền file → Result trả về user — tất cả hoạt động đúng.

======

## time: 08:45

### Mục tiêu
Bổ sung phần hướng dẫn host local workers vào E2E test runner skill, để có thể test real OCR pipeline end-to-end (không chỉ simulated worker).

### Thay đổi

**`.claude/skills/e2e-test-runner/SKILL.md` — rewrite toàn bộ:**
- Thêm **Phase 1 — Start Local Workers**: hướng dẫn chi tiết chạy 3 loại worker local (Tesseract, PaddleOCR, PaddleOCR-VL)
- Thêm bảng Worker Registry: service_type, NATS subject, engine, GPU, script path
- Thêm reference SEED_SERVICES env var và cách pre-approve services
- Thêm 3 options start worker: run-local.sh, all-in-background, custom env vars
- Thêm bảng env vars reference cho mỗi worker type
- Thêm Worker Lifecycle diagram (register → connect NATS → heartbeat → job loop)
- Thêm verify workers, stop workers, troubleshooting table
- Thêm test patterns cho Real OCR (P3 scenarios: E2E-016→020)
- Thêm conftest fixtures: `requires_tesseract`, `requires_paddle`, `requires_any_ocr_worker`
- Thêm full stack startup script (infra + backend + admin + worker + verify)
- Cập nhật Mode table: thêm "Real OCR" mode
- Cập nhật test run commands: `pytest -m real_ocr`, `pytest -m "not real_ocr"`

### Lý do
Skill cũ chỉ hướng dẫn test với simulated worker (gọi internal API). Bổ sung hướng dẫn start real workers (Tesseract/PaddleOCR/PaddleVL) để có thể test full OCR pipeline: upload ảnh thật → worker OCR thật → kết quả text thật.

======

## time: 15:50

### Mục tiêu
Đổi MinIO object key format từ UUID-based sang human-readable, traceable format.

### Thay đổi

**`02_backend/app/infrastructure/storage/utils.py` — rewrite 3 functions:**
- `generate_object_key()`: Cũ `{user_id}/{request_id}/{file_id}/{name}` → Mới `{YYYY-MM-DD}/{HHmmss}_{method}_{user_short8}/{name}`
- `generate_result_key()`: Cũ `{user_id}/{request_id}/{file_id}/result.{fmt}` → Mới `{YYYY-MM-DD}/{HHmmss}_{method}_{user_short8}/{basename}_result.{fmt}`
- `parse_object_key()`: Updated để parse format mới
- Thêm `_sanitize_filename()`: Loại bỏ path traversal, giới hạn 200 chars
- Thêm params mới: `method`, `created_at`, `original_name`

**`02_backend/app/modules/upload/service.py`:**
- `generate_object_key()` call: thêm `method=method, created_at=request.created_at`

**`02_backend/app/modules/file_proxy/service.py`:**
- `generate_result_key()` call: thêm `original_name=file.original_name, method=request.method, created_at=request.created_at`

**Tests cập nhật:**
- `00_test/backend/test_storage_helpers.py` — SH-001→003: assertions cho format mới + test sanitize filename
- `00_test/backend/test_file_proxy.py` — FP-005: thêm expected params `original_name`, `method`, `created_at`
- `00_test/backend/conftest.py` — `make_request()`: thêm `created_at` field

### Kết quả
- Backend unit tests: 172 passed ✅
- Contract tests: 122 passed ✅
- E2E tests: 40 passed ✅

### Ví dụ path mới
```
uploads bucket: 2026-03-15/153042_ocr_text_raw_4d1df76a/invoice_scan.png
results bucket: 2026-03-15/153042_ocr_text_raw_4d1df76a/invoice_scan_result.txt
```

### Lý do
Path cũ toàn UUID (`4d1df76a-ec40-48af.../a8f3b2c1-1234.../c9d8e7f6-.../file.png`) không thể trace khi debug trong MinIO browser. Format mới cho phép: browse theo ngày, nhận diện method+user ngay từ folder name, thấy filename gốc.

======

## time: 19:00

### Mục tiêu
Setup PaddleOCR và PaddleOCR-VL chạy local bằng GPU (RTX 3070) thay vì CPU.

### Thay đổi

**Conda env `ocr-gpu` (mới):**
- Tạo conda env `ocr-gpu` (Python 3.12) chuyên cho OCR workers GPU
- Cài `paddlepaddle-gpu==3.2.2` (CUDA 12.6) từ PaddlePaddle CN index
- Cài `paddleocr==3.4.0` + `paddlex[ocr]==3.4.2` + tất cả worker dependencies
- Upgrade nvidia packages (NCCL, cuBLAS, cuDNN...) lên phiên bản compatible với torch trong user site-packages
- Base env giữ nguyên `paddlepaddle==3.2.2` (CPU) + torch 2.10.0

**Run scripts cập nhật:**
- `03_worker/deploy/paddle-text/run-local.sh` — `conda activate base` → `conda activate ocr-gpu`, thêm `PADDLE_PDX_DISABLE_MODEL_SOURCE_CHECK=True`
- `03_worker/deploy/paddle-vl/run-local.sh` — tương tự

### Kết quả
- PaddleOCR (text): GPU OK, ~129 MB VRAM
- PPStructureV3 (VL): GPU OK, ~3556 MB VRAM
- Tổng VRAM nếu chạy đồng thời: ~3.7 GB / 8 GB (RTX 3070)
- Base env không bị ảnh hưởng (torch vẫn hoạt động)

### Lý do
Workers đang chạy CPU (paddlepaddle 3.2.2 CPU trong base env). Máy có RTX 3070 nhưng chưa tận dụng. Tạo env riêng `ocr-gpu` để tránh conflict giữa paddlepaddle-gpu và torch (NCCL version mismatch).
