date: 2026-03-10
time: 17:00 (ICT)
======

## Bugfix: Service instance re-registration không reset trạng thái DEAD

### Triệu chứng

```
FAIL: 400 - {"detail":"No approved service available for method 'ocr_text_raw' tier 0"}
```

Service `ocr-paddle-vl` đã được admin approve nhưng upload vẫn bị từ chối. Test script cũng báo 404 khi check available services.

---

### Root Cause Analysis

**Bug 1 (Critical) — `ServiceInstanceRepository.register()` không reset status khi re-register**

Luồng gây ra bug:

```
Worker khởi động lần đầu → register() → instance WAITING
Admin approve → instance WAITING → ACTIVE           (OK)
Worker bị heartbeat monitor đánh dấu DEAD (timeout)
Worker restart → register() lại với cùng instance_id
  → existing record tìm thấy
  → chỉ update last_heartbeat_at
  → status vẫn giữ nguyên DEAD                     (BUG)
_validate_service_available() → 0 active instances → raise ServiceNotAvailable
```

File lỗi: `infrastructure/database/repositories/service_instance.py:62-68`

Code cũ:
```python
if existing:
    existing.last_heartbeat_at = datetime.now(timezone.utc)
    if metadata:
        existing.instance_metadata = json.dumps(metadata)
    return self.update(existing)  # ← status không được reset
```

**Bug 2 (Minor) — Test script gọi sai endpoint**

`05_test_cases/test_api_flow.py:38` gọi `GET /services` nhưng endpoint thực tế là `GET /services/available`.

---

### Fix

**Fix 1** — `repositories/service_instance.py`

Khi existing instance re-register và service type là APPROVED: reset status DEAD/WAITING → ACTIVE.

```python
if existing:
    existing.last_heartbeat_at = datetime.now(timezone.utc)
    if metadata:
        existing.instance_metadata = json.dumps(metadata)
    # Re-activation: if type is APPROVED, a re-registering instance should become ACTIVE
    if service_type.status == ServiceTypeStatus.APPROVED:
        if existing.status in (ServiceInstanceStatus.DEAD, ServiceInstanceStatus.WAITING):
            existing.status = ServiceInstanceStatus.ACTIVE
    return self.update(existing)
```

**Fix 2** — `05_test_cases/test_api_flow.py`

```python
# Trước
resp = client.get("/services")
# Sau
resp = client.get("/services/available")
```

---

### Files thay đổi

| File | Loại | Thay đổi |
|------|:----:|----------|
| `infrastructure/database/repositories/service_instance.py` | Fix | Re-registration reset status DEAD/WAITING → ACTIVE khi type APPROVED |
| `05_test_cases/test_api_flow.py` | Fix | Sửa endpoint `/services` → `/services/available` |

---

### Lưu ý vận hành

Nếu sau fix vẫn gặp lỗi tương tự, kiểm tra:

1. Worker container có đang chạy không:
   ```bash
   docker compose logs ocr-paddle-vl --tail=50
   ```
2. Xem danh sách instances qua admin:
   ```
   GET /api/v1/admin/service-instances
   ```
   Nếu list trống → worker chưa bao giờ gọi `/internal/register` → cần start worker.

3. Xác nhận service type có đúng `allowed_methods` và `allowed_tiers`:
   ```
   GET /api/v1/admin/service-types/{id}
   ```
   Phải có `"ocr_text_raw"` trong `allowed_methods` và `0` trong `allowed_tiers`.

date: 2026-03-10
time: 18:00 (ICT)
======

## Thêm logging vào tất cả try/except blocks — Backend

### Mục tiêu

Đảm bảo mỗi except block đều emit log để dễ debug khi có lỗi trong production. Trước đây nhiều block dùng `pass` hoặc raise HTTPException mà không log gì.

---

### Files thay đổi — 8 files, 15 log calls

| File | Blocks | Log level | Ghi chú |
|------|:------:|-----------|---------|
| `api/v1/endpoints/upload.py` | 2 | `WARNING`, `ERROR` | Thêm `import logging` + logger. ERROR dùng `exc_info=True` để có stack trace |
| `api/v1/endpoints/jobs.py` | 4 | `DEBUG`, `WARNING` | Thêm logger. WARNING khi file không tìm thấy trong storage, DEBUG khi parse fail |
| `api/v1/endpoints/auth.py` | 2 | `INFO`, `WARNING` | Thêm logger. INFO cho user exists (expected), WARNING cho invalid credentials |
| `api/v1/endpoints/files.py` | 2 | `WARNING` | Thêm logger. WARNING khi original/result file không tìm thấy trong MinIO |
| `api/v1/endpoints/admin/service_types.py` | 4 | `WARNING` | Logger đã có. Thêm log trước raise trong approve/reject/disable/enable |
| `infrastructure/database/connection.py` | 1 | `DEBUG` | Logger đã có. Migration errors vốn expected (duplicate column) → DEBUG |
| `modules/job/orchestrator.py` | 1 | `DEBUG` | Logger đã có. Thay `pass` bằng `logger.debug` khi parse error_history thất bại |
| `infrastructure/queue/nats_client.py` | 1 | `WARNING` | Logger đã có. Thay `pass` bằng `logger.warning` khi tạo NATS stream thất bại |

---

### Quy tắc log level áp dụng

| Level | Khi nào dùng |
|-------|-------------|
| `ERROR` + `exc_info=True` | Exception không mong đợi, cần stack trace để debug |
| `WARNING` | Lỗi business logic / resource không tìm thấy / thao tác thất bại |
| `INFO` | Sự kiện bình thường nhưng đáng ghi nhận (vd: user đã tồn tại khi register) |
| `DEBUG` | Parse fail có fallback, migration skip — không ảnh hưởng flow |

---

time: 19:00 (ICT)
======

## Solution Architecture Review

### Mục tiêu
Đánh giá toàn diện kiến trúc codebase (Phase 1 Complete) theo vai trò Solution Architect.

### Files thay đổi
- `04_docs/evaluation/architecture-review-2026-03-10.md` — Tạo mới: báo cáo đánh giá kiến trúc

### Nội dung đánh giá
Phân tích 8 khía cạnh: high-level architecture, module organization, coupling, data flow, database, scalability, error handling, API design.

**Kết quả tổng: 33/50 — ACCEPTABLE → GOOD**

**11 vấn đề phát hiện:**
- HIGH: SQLite là SPOF (cần backup + plan migrate PostgreSQL), Migration strategy fragile (cần Alembic), Secret management không an toàn, Thiếu observability stack
- MEDIUM: APScheduler không scale được khi multi-instance, CORS config rủi ro, Audit log chưa đầy đủ, Thiếu circuit breaker cho worker HTTP clients, File preview không có size limit
- LOW: Worker auto-deregistration chậm, Frontend thiếu 401 interceptor

**Lý do:** Tài liệu hóa trạng thái kiến trúc sau Phase 1 để làm baseline cho planning Phase 2+, cung cấp input cho technical debt prioritization.

---

time: 20:15 (ICT)
======

## Plan: Loki + Grafana Observability Stack cho OCR Platform

### Bối cảnh

Architecture Review đánh giá "Thiếu observability stack" là vấn đề **HIGH**. Backend đã output structured JSON logs ra stdout (qua `core/logging.py` — `JSONFormatter`), nhưng hiện tại chỉ xem được qua `docker compose logs`. Cần centralized log aggregation để:

- Tìm kiếm/lọc log theo `request_id`, `job_id`, `instance_id`, `service_type`
- Trace full flow: upload → job → worker heartbeat → result
- Alert khi có ERROR spike hoặc service down
- Giữ log lâu dài (hiện Docker chỉ giữ theo rotation policy)

### Stack chọn

```
Container stdout/stderr
      ↓
  Promtail  (collector — đọc Docker logs, gắn labels)
      ↓
    Loki    (storage — index labels, store log chunks)
      ↓
  Grafana   (UI — query, dashboard, alert)
```

**Lý do chọn Loki thay vì ELK:**
- Lightweight hơn nhiều (Loki không full-text index, chỉ index labels)
- Phù hợp self-host nhỏ, ít RAM (Loki ~256MB vs Elasticsearch ~2GB+)
- Grafana là single UI cho cả logs + metrics (sau này thêm Prometheus)
- Cùng ecosystem với Prometheus (khi scale lên)

### Kiến trúc tích hợp

```
┌──────────────────── ocr-network ────────────────────┐
│                                                      │
│  ┌─────────┐  ┌──────┐  ┌─────────┐  ┌──────────┐  │
│  │ backend │  │ nats │  │  minio  │  │ frontend │  │
│  └────┬────┘  └──┬───┘  └────┬────┘  └────┬─────┘  │
│       │          │           │             │         │
│       └──────────┴───────────┴─────────────┘         │
│                      │ Docker logs                   │
│                      ▼                               │
│              ┌──────────────┐                        │
│              │   Promtail   │  (mount docker.sock)   │
│              └──────┬───────┘                        │
│                     │ push                           │
│                     ▼                                │
│              ┌──────────────┐                        │
│              │     Loki     │  (port 3100)           │
│              └──────┬───────┘                        │
│                     │ query                          │
│                     ▼                                │
│              ┌──────────────┐                        │
│              │   Grafana    │  (port 3001)           │
│              └──────────────┘                        │
└──────────────────────────────────────────────────────┘
```

### Kế hoạch triển khai — 4 bước

#### Bước 1: Thêm Loki + Grafana + Promtail vào docker-compose

Thêm 3 services mới vào `docker-compose.yml`:

```yaml
  # === OBSERVABILITY ===
  loki:
    image: grafana/loki:3.0.0
    container_name: ocr-loki
    ports:
      - "3100:3100"
    volumes:
      - ./data/loki:/loki
      - ./config/loki.yml:/etc/loki/local-config.yaml
    command: -config.file=/etc/loki/local-config.yaml
    networks:
      - ocr-network

  promtail:
    image: grafana/promtail:3.0.0
    container_name: ocr-promtail
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - ./config/promtail.yml:/etc/promtail/config.yml
    command: -config.file=/etc/promtail/config.yml
    depends_on:
      - loki
    networks:
      - ocr-network

  grafana:
    image: grafana/grafana:11.0.0
    container_name: ocr-grafana
    ports:
      - "${GRAFANA_PORT:-3001}:3000"
    environment:
      - GF_SECURITY_ADMIN_USER=${GRAFANA_USER:-admin}
      - GF_SECURITY_ADMIN_PASSWORD=${GRAFANA_PASSWORD:-admin}
      - GF_AUTH_ANONYMOUS_ENABLED=false
    volumes:
      - ./data/grafana:/var/lib/grafana
      - ./config/grafana/provisioning:/etc/grafana/provisioning
    depends_on:
      - loki
    networks:
      - ocr-network
```

#### Bước 2: Tạo config files

**`config/loki.yml`** — Loki config (single-instance, filesystem storage):

```yaml
auth_enabled: false

server:
  http_listen_port: 3100

common:
  path_prefix: /loki
  storage:
    filesystem:
      chunks_directory: /loki/chunks
      rules_directory: /loki/rules
  replication_factor: 1
  ring:
    kvstore:
      store: inmemory

schema_config:
  configs:
    - from: 2024-01-01
      store: tsdb
      object_store: filesystem
      schema: v13
      index:
        prefix: index_
        period: 24h

limits_config:
  retention_period: 720h  # 30 ngày

compactor:
  working_directory: /loki/compactor
  retention_enabled: true
```

**`config/promtail.yml`** — Promtail config (Docker service discovery):

```yaml
server:
  http_listen_port: 9080

positions:
  filename: /tmp/positions.yaml

clients:
  - url: http://loki:3100/loki/api/v1/push

scrape_configs:
  - job_name: docker
    docker_sd_configs:
      - host: unix:///var/run/docker.sock
        refresh_interval: 5s
        filters:
          - name: network
            values: ["ocr-network"]
    relabel_configs:
      # Lấy container name làm label
      - source_labels: ['__meta_docker_container_name']
        regex: '/(.*)'
        target_label: 'container'
      # Lấy service name từ compose
      - source_labels: ['__meta_docker_container_label_com_docker_compose_service']
        target_label: 'service'
    pipeline_stages:
      # Parse JSON log từ backend
      - match:
          selector: '{service="backend"}'
          stages:
            - json:
                expressions:
                  level: level
                  request_id: request_id
                  job_id: job_id
                  instance_id: instance_id
                  service_type: service_type
                  logger_name: logger
            - labels:
                level:
                logger_name:
            # Đưa structured fields vào labels để query được
            - structured_metadata:
                request_id:
                job_id:
                instance_id:
                service_type:
```

**`config/grafana/provisioning/datasources/loki.yml`** — Auto-provision Loki datasource:

```yaml
apiVersion: 1
datasources:
  - name: Loki
    type: loki
    access: proxy
    url: http://loki:3100
    isDefault: true
    editable: true
```

#### Bước 3: Thêm entries vào .gitignore

```
data/loki/
data/grafana/
```

#### Bước 4: Tạo Grafana dashboards (provisioned JSON)

Tạo sẵn dashboard cho các use case chính:

| Dashboard | Panels | Mục đích |
|-----------|--------|----------|
| **OCR Overview** | Log volume by level, Error rate, Top loggers | Tổng quan health |
| **Request Trace** | Filter by `request_id`, timeline view | Trace 1 request end-to-end |
| **Job Monitor** | Filter by `job_id`, `service_type`, error logs | Debug job failures |
| **Worker Health** | Filter by `instance_id`, heartbeat logs, registration logs | Monitor worker status |

---

### Useful Grafana queries (LogQL)

```logql
# Tất cả ERROR logs từ backend
{service="backend"} | json | level="ERROR"

# Trace 1 request cụ thể
{service="backend"} | json | request_id="<uuid>"

# Logs cho 1 job cụ thể
{service="backend"} | json | job_id="<uuid>"

# Upload failures (lỗi mà user đang gặp)
{service="backend"} |= "Upload rejected" or |= "No service available"

# Worker heartbeat issues
{service="backend"} | json | logger_name="app.api.v1.internal.heartbeat"

# Error rate (dùng cho alert)
sum(rate({service="backend"} | json | level="ERROR" [5m]))
```

---

### Thứ tự triển khai

```
Bước 1: Tạo config files (loki.yml, promtail.yml, grafana provisioning)
    ↓
Bước 2: Update docker-compose.yml + .gitignore
    ↓
Bước 3: docker compose up -d → verify Grafana UI tại :3001
    ↓
Bước 4: Import/tạo dashboards
```

### Dependencies mới: 0 code changes

Toàn bộ chỉ là infra config. Backend không cần sửa gì vì đã output JSON logs ra stdout.

### Ghi chú

- Promtail mount `docker.sock` read-only — chỉ đọc container logs, không control containers
- Loki retention 30 ngày — đủ cho debug, không tốn nhiều disk
- Grafana port `3001` để không conflict với frontend (`3000`)
- Sau này mở rộng: thêm Prometheus + node_exporter cho metrics (CPU, RAM, request latency)

---

time: 21:30 (ICT)
======

## Fix: Frontend không hiển thị OCR service engine

### Vấn đề

Frontend không hiển thị tên OCR engine (PaddleOCR, Tesseract...) ở bất kỳ đâu. Backend lưu `engine_info` (name, version, capabilities) trong ServiceType nhưng:
- Public API `GET /services/available` không trả `engine_info`
- AdminServicesPage fetch `engine_info` nhưng không render
- UploadConfig chỉ show method/tier, không hiển thị engine nào sẽ xử lý
- ResultMetadata show version nhưng không có engine name
- Bug phụ: frontend `JobResultMetadata.version` không match backend `service_version` → hiển thị `vundefined`

### Fix

**Backend (2 files):**
- `api/v1/endpoints/services.py` — Thêm `engine_info` vào `AvailableServiceResponse`, parse JSON và trả trong public API
- `api/v1/schemas/job.py` — Thêm `engine_name` vào `JobResultMetadata`
- `api/v1/endpoints/jobs.py` — Lookup service type từ `job.worker_id` để resolve `engine_name` từ `engine_info.name`

**Frontend (6 files):**
- `api/services.ts` — Thêm `EngineInfo` interface, thêm `engine_info` vào `AvailableService`
- `types/job.ts` — Fix `version` → `service_version` (match backend), thêm `engine_name`
- `components/upload/UploadConfig.tsx` — Hiển thị engine name + version trong Active Model badge
- `components/result/ResultMetadata.tsx` — Thêm engine name badge, fix `metadata.version` → `metadata.service_version`
- `pages/ResultViewerPage.tsx` — Hiển thị engine name trong footer bar
- `pages/AdminServicesPage.tsx` — Thêm cột "Engine" hiển thị engine name + version

### Files thay đổi

| File | Loại | Thay đổi |
|------|:----:|----------|
| `02_backend/app/api/v1/endpoints/services.py` | Feature | Trả `engine_info` trong public API |
| `02_backend/app/api/v1/schemas/job.py` | Feature | Thêm `engine_name` vào `JobResultMetadata` |
| `02_backend/app/api/v1/endpoints/jobs.py` | Feature | Resolve engine name từ ServiceType cho job result |
| `01_frontend/src/api/services.ts` | Feature | Thêm `EngineInfo` type, update `AvailableService` |
| `01_frontend/src/types/job.ts` | Bugfix | Fix `version` → `service_version`, thêm `engine_name` |
| `01_frontend/src/components/upload/UploadConfig.tsx` | Feature | Show engine name trong Active Model badge |
| `01_frontend/src/components/result/ResultMetadata.tsx` | Bugfix+Feature | Fix version field, thêm engine name badge |
| `01_frontend/src/pages/ResultViewerPage.tsx` | Feature | Show engine name trong result footer |
| `01_frontend/src/pages/AdminServicesPage.tsx` | Feature | Thêm cột Engine trong service table |

---

time: 22:30 (ICT)
======

## Thêm Dockerfile GPU base cho CUDA 12 (RTX 50xx / Blackwell)

### Bối cảnh

Base GPU image hiện tại (`Dockerfile.base-gpu`) dùng `nvidia/cuda:11.8.0-cudnn8-runtime-ubuntu22.04` — chỉ tương thích với GPU cũ (RTX 20xx/30xx). Máy Windows mới dùng RTX 5060 Ti (Blackwell architecture, CUDA 13.0, driver 581.29) cần CUDA 12.x+ trong container.

### Giải pháp

Tạo bộ Dockerfile song song `*-cu12` — cùng cấu trúc, chỉ khác CUDA version và PaddlePaddle version:

- **Base image**: `nvidia/cuda:12.6.3-cudnn-runtime-ubuntu22.04`
- **PaddlePaddle**: 3.0.0 (từ CUDA 12.3 index của PaddlePaddle)
- **PaddleOCR**: 2.9.1 (tương thích PaddlePaddle 3.x)

### Files thay đổi

| File | Loại | Thay đổi |
|------|:----:|----------|
| `03_worker/Dockerfile.base-gpu-cu12` | New | Base GPU image CUDA 12.6 + Python 3.11 |
| `03_worker/deploy/paddle-text/Dockerfile.cu12` | New | Paddle text worker dùng cu12 base |
| `03_worker/deploy/paddle-text/requirements-cu12.txt` | New | PaddlePaddle 3.0.0 + PaddleOCR 2.9.1 |
| `03_worker/deploy/paddle-text/docker-compose.cu12.yml` | New | Compose file cho paddle text CUDA 12 |
| `03_worker/deploy/paddle-vl/Dockerfile.cu12` | New | Paddle VL worker dùng cu12 base |
| `03_worker/deploy/paddle-vl/requirements-cu12.txt` | New | PaddlePaddle 3.0.0 + PaddleOCR 2.9.1 |
| `03_worker/deploy/paddle-vl/docker-compose.cu12.yml` | New | Compose file cho paddle VL CUDA 12 |
| `03_worker/Makefile` | Update | Thêm targets: `build-base-gpu-cu12`, `build-paddle-cu12`, `up-paddle-cu12`, v.v. |

### Cách dùng

```bash
cd 03_worker

# Build + run PaddleOCR (CUDA 12 — cho Windows RTX 50xx)
make up-paddle-cu12
make up-paddle-vl-cu12

# Build + run PaddleOCR (CUDA 11.8 — cho Ubuntu GPU cũ)
make up-paddle
make up-paddle-vl
```

---

time: 23:00 (ICT)
======

## Thêm File Logging cho Backend & Worker

### Mục tiêu

Lưu log vào file để review lại khi cần, debug production issues, giữ lịch sử log bên cạnh stdout. Bổ sung cho Loki stack (khi chạy local không có Docker).

### Thay đổi

**Backend — `02_backend/app/core/logging.py`:**
- Thêm `TimedRotatingFileHandler` ghi vào `data/logs/backend.log`
- Rotation: midnight, giữ 30 ngày (`backupCount=30`)
- Dùng chung `JSONFormatter` với stdout handler
- File cũ tự đổi tên: `backend.log.2026-03-09`, `backend.log.2026-03-08`...

**Worker — `03_worker/app/main.py`:**
- Thêm `TimedRotatingFileHandler` ghi vào `data/logs/worker.log`
- Rotation: midnight, giữ 30 ngày (`backupCount=30`)
- Dùng chung plain-text formatter với stdout handler
- File cũ tự đổi tên: `worker.log.2026-03-09`, `worker.log.2026-03-08`...

**`.gitignore`** — Không cần sửa: đã có `data/` và `*.log`.

### Files thay đổi

| File | Loại | Thay đổi |
|------|:----:|----------|
| `02_backend/app/core/logging.py` | Feature | Thêm `TimedRotatingFileHandler`, giữ stdout handler |
| `03_worker/app/main.py` | Feature | Thêm `TimedRotatingFileHandler`, giữ stdout handler |

### Verification

- Chạy backend → `data/logs/backend.log` được tạo + stdout vẫn in JSON
- Chạy worker → `data/logs/worker.log` được tạo + stdout vẫn in plain text
- Sau midnight → file cũ được rename, file mới được tạo tự động
