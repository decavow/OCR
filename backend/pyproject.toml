[project]
name = "ocr-platform-backend"
version = "1.0.0"
description = "OCR Platform Backend - FastAPI"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"

[tool.ruff]
line-length = 100
target-version = "py311"
