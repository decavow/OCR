# OCR Worker - PaddleOCR-VL Structured Extraction (GPU)
#
# Prerequisites: build base image first
#   cd 03_worker && make build-base-gpu
#
# Usage (from project root):
#   docker compose -f 03_worker/deploy/paddle-vl/docker-compose.yml up -d
#   docker compose -f 03_worker/deploy/paddle-vl/docker-compose.yml down

services:
  worker-paddle-vl:
    build:
      context: ../..
      dockerfile: deploy/paddle-vl/Dockerfile
    image: ocr-worker-paddle-vl:latest
    env_file:
      - ../../../.env
      - .env
    stop_grace_period: 60s
    networks:
      - ocr-network
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]

networks:
  ocr-network:
    external: true
    name: ocr-network
